{
  "name": "@ode-internal/shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pre-built shell UI for ode. Bundled into dist/shell/ at build time. Not published.",
  "scripts": {
    "build": "vite build"
  }
}
